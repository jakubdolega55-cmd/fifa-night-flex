FIFA Night Flex 1.1.2 - Awards checkpoint

Changes:
1) Organizer laureate dropdown shows each candidate's category-specific ranking stats/reason.
2) Saved laureate can be removed/reset per category and selected again.

Files changed:
- app.py
- database.py

Syntax validation: python py_compile PASS for app.py + database.py.
This is a checkpoint, not the final 1.1.2 release package.
